// A more professional approach is to use a button rather then a link which we study in the last chapter.

function greetWithMessage () {
    let message = prompt("Enter the state");
    alert(message);
} 