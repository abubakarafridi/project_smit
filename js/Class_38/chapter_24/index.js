// let firstChar = firstName.charAt(0)

// The code above finds a single character at index-0 (the beginning) of the string
// represented by the variable firstName and assigns it to the variable firstChar.

// The following code finds the last character in the string.

// var lastChar = name.charAt(name.length - 1);

// The following code cycles through a string looking for an exclamation point. If the character is found, an alert displays.

// let text = "Hi bro!";
// console.log(text);
// for (let i = 0; i < text.length; i++) {
//     if (text.charAt(i) === "!") {
//         console.log("Exclamation point found!");
//         break;
//     }
// }